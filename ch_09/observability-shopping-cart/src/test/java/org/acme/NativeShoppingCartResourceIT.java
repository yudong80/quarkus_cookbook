package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeShoppingCartResourceIT extends ShoppingCartResourceTest {

    // Execute the same tests but in native mode.
}